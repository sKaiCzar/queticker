import * as React from 'react';
import * as ReactDOM from 'react-dom';
import AppHelper from './AppHelper.jsx';

function render() {
  ReactDOM.render(
    <AppHelper />,
    document.body);
}

render();
